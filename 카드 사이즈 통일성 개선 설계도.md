# 캐릭터 상세보기 카드 사이즈 통일성 개선 설계

## 📋 현재 상황 분석

### 현재 레이아웃 구조

```
┌─────────────────────────────────────────────────────────────────┐
│  3-Column Grid Layout (Desktop)                                  │
├──────────────┬──────────────────────┬────────────────────────────┤
│  Left (280px)│  Center (400px~1fr) │  Right (400px)             │
├──────────────┼──────────────────────┼────────────────────────────┤
│              │                      │  MainStatsCard             │
│ ProfileSection│  EquipmentGrid      │  (높이: 가변)              │
│              │                      ├────────────────────────────┤
│ (높이: 가변) │  (높이: 가변)        │  RankingCard               │
│              │                      │  (높이: 가변)              │
│              │                      ├────────────────────────────┤
│              │                      │  TitleCard                 │
│              │                      │  (높이: 가변)              │
│              │                      ├────────────────────────────┤
│              │                      │  DaevanionCard             │
│              │                      │  (높이: 가변)              │
└──────────────┴──────────────────────┴────────────────────────────┘
```

### 문제점

1. **높이 불일치**: ProfileSection과 우측 4개 카드의 총 높이가 맞지 않음
2. **사이즈 통일성 부족**: 각 카드의 패딩, 간격, 폰트 크기가 제각각
3. **비율 비효율**: 우측 카드들이 공간을 효율적으로 활용하지 못함
4. **반응형 미흡**: 작은 화면에서 카드들이 깨짐

---

## 🎯 설계 목표

### 핵심 원칙

1. **ProfileSection을 기준점으로 설정**
   - ProfileSection의 총 높이를 계산
   - 우측 4개 카드의 총 높이 = ProfileSection 높이

2. **4개 카드의 높이 비율 설정**
   - MainStatsCard: 35% (가장 중요)
   - RankingCard: 35% (정보량 많음)
   - TitleCard: 18%
   - DaevanionCard: 12%

3. **일관된 디자인 시스템**
   - 패딩: 모든 카드 동일 (1rem)
   - 간격: 카드 간 1rem
   - 폰트 크기: 3단계 (제목, 본문, 보조)

4. **가독성 우선**
   - 최소 폰트 크기: 0.7rem
   - 아이콘 최소 크기: 20px
   - 터치 영역: 최소 44px

---

## 📐 구체적인 사이즈 설계

### ProfileSection 높이 계산

현재 ProfileSection 구성 요소:
```
- 프로필 이미지: 120px
- S1 배지 공간: ~30px
- 이름/레벨: ~80px
- Divider: 1px
- Combat Power: ~100px
- 랭킹 정보 (3개): ~190px
- 서버/종족 정보: ~70px
- 패딩 (상하): 48px (1.5rem * 2)
─────────────────────────
예상 총 높이: ~639px
```

### 우측 카드 높이 배분 (총 639px 기준)

```
총 사용 가능 높이 = ProfileSection 높이 - (카드 간 간격 * 3)
                   = 639px - (24px * 3)
                   = 567px

┌────────────────────────────────┐
│ MainStatsCard (35%)            │
│ 높이: 198px                    │  ← 주요 스텟 6개
├────────────────────────────────┤
│ RankingCard (35%)              │
│ 높이: 198px                    │  ← 랭킹 7개
├────────────────────────────────┤
│ TitleCard (18%)                │
│ 높이: 102px                    │  ← 타이틀 3개 카테고리
├────────────────────────────────┤
│ DaevanionCard (12%)            │
│ 높이: 69px                     │  ← 주신 6개
└────────────────────────────────┘
```

---

## 🎨 각 카드별 상세 설계

### 1. MainStatsCard (198px 고정)

#### 현재 구조
- 헤더: "주요 스텟" (1.125rem)
- 6개 스텟 그리드 (repeat(6, 1fr))
- 아이콘 28px, 라벨 0.75rem, 값 1rem
- 더보기 버튼

#### 개선안 (198px 내 최적화)
```css
.main-stats-card {
  height: 198px;
  padding: 1rem;
  box-sizing: border-box;
}

/* 헤더 (20px) */
.stats-header {
  font-size: 0.95rem;  /* 1.125rem → 0.95rem */
  margin-bottom: 0.75rem;
  height: 20px;
}

/* 스텟 그리드 (140px) */
.stats-grid {
  display: grid;
  grid-template-columns: repeat(6, 1fr);
  gap: 0.4rem;  /* 0.5rem → 0.4rem */
  height: 140px;
}

/* 각 스텟 아이템 */
.stat-item {
  padding: 0.5rem 0.2rem;  /* 0.75rem 0.25rem → 축소 */
}

.stat-icon {
  width: 22px;  /* 28px → 22px */
  height: 22px;
  margin-bottom: 0.35rem;  /* 0.5rem → 0.35rem */
}

.stat-label {
  font-size: 0.7rem;  /* 0.75rem → 0.7rem */
  margin-bottom: 0.2rem;
}

.stat-value {
  font-size: 0.85rem;  /* 1rem → 0.85rem */
}

/* 더보기 버튼 (32px) */
.expand-button {
  height: 32px;
  font-size: 0.8rem;  /* 0.875rem → 0.8rem */
  margin-top: 0.5rem;
}
```

**변경 포인트:**
- ✅ 아이콘 크기 축소: 28px → 22px
- ✅ 폰트 크기 조정: 제목 0.95rem, 라벨 0.7rem, 값 0.85rem
- ✅ 패딩 최적화: 공간 효율성 증가
- ✅ 가독성 유지: 최소 폰트 크기 0.7rem 준수

---

### 2. RankingCard (198px 고정)

#### 현재 구조
- 헤더: "랭킹 정보" (1.125rem)
- 7개 랭킹 아이템 (각 아이템 높이 가변)
- 아이콘 32px, 라벨 0.9rem

#### 개선안 (198px 내 최적화)
```css
.ranking-card {
  height: 198px;
  padding: 1rem;
  box-sizing: border-box;
}

/* 헤더 (20px) */
.ranking-header {
  font-size: 0.95rem;  /* 1.125rem → 0.95rem */
  margin-bottom: 0.75rem;
  height: 20px;
}

/* 랭킹 리스트 (162px) */
.ranking-list {
  display: flex;
  flex-direction: column;
  gap: 0.4rem;  /* 0.75rem → 0.4rem */
  height: 162px;
  overflow-y: auto;  /* 스크롤 가능 */
}

/* 각 랭킹 아이템 (20px) */
.ranking-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0.4rem;  /* 0.75rem → 0.4rem */
  min-height: 20px;  /* 최소 높이 보장 */
}

/* 아이콘 */
.ranking-icon {
  width: 24px;  /* 32px → 24px */
  height: 24px;
}

.ranking-icon-dot {
  width: 6px;  /* 8px → 6px */
  height: 6px;
}

/* 라벨 */
.ranking-label {
  font-size: 0.8rem;  /* 0.9rem → 0.8rem */
}

/* 값 */
.ranking-rank {
  font-size: 0.85rem;  /* 0.95rem → 0.85rem */
}

.ranking-detail {
  font-size: 0.7rem;  /* 0.75rem → 0.7rem */
}
```

**변경 포인트:**
- ✅ 아이템 간격 축소: 0.75rem → 0.4rem
- ✅ 아이콘 크기 축소: 32px → 24px
- ✅ 패딩 최적화: 0.75rem → 0.4rem
- ✅ 스크롤 추가: 7개 항목이 넘치면 스크롤 가능
- ✅ 가독성 유지: 폰트 크기 0.7rem 이상

---

### 3. TitleCard (102px 고정)

#### 현재 구조
- 헤더: "타이틀" + 소유/전체 개수 (1.125rem)
- 3개 카테고리 그리드 (repeat(3, 1fr))
- 아이콘 24px, 타이틀명, 효과 텍스트

#### 개선안 (102px 내 최적화) - **컴팩트 모드**

```css
.title-card {
  height: 102px;
  padding: 1rem;
  box-sizing: border-box;
}

/* 헤더 (24px) */
.title-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 0.5rem;  /* 1.25rem → 0.5rem */
  height: 24px;
}

.title-header h3 {
  font-size: 0.9rem;  /* 1.125rem → 0.9rem */
}

.title-count {
  font-size: 0.75rem;  /* 0.875rem → 0.75rem */
  padding: 0.2rem 0.5rem;  /* 0.25rem 0.75rem → 축소 */
}

/* 카테고리 그리드 (62px) */
.category-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0.4rem;  /* 0.75rem → 0.4rem */
  height: 62px;
}

/* 각 카테고리 카드 */
.category-card {
  padding: 0.4rem;  /* 0.875rem → 0.4rem */
  display: flex;
  flex-direction: column;
  justify-content: center;
}

/* 아이콘 */
.category-icon svg {
  width: 16px;  /* 24px → 16px */
  height: 16px;
}

/* 카테고리명 */
.category-name {
  font-size: 0.7rem;  /* 0.875rem → 0.7rem */
  font-weight: bold;
}

/* 개수 */
.category-count {
  font-size: 0.7rem;  /* 0.75rem → 0.7rem */
  margin-bottom: 0.3rem;  /* 0.75rem → 0.3rem */
}

/* 대표 타이틀 - 생략 또는 간소화 */
.representative-title {
  display: none;  /* 공간 부족으로 숨김 */
}

/* 대안: 호버 시 툴팁으로 표시 */
.category-card:hover::after {
  content: attr(data-title);
  position: absolute;
  background: rgba(17, 19, 24, 0.95);
  padding: 0.5rem;
  border: 1px solid #1F2433;
  border-radius: 6px;
  font-size: 0.75rem;
  z-index: 10;
  /* ... 툴팁 스타일 */
}
```

**변경 포인트:**
- ✅ 아이콘 크기 축소: 24px → 16px
- ✅ 대표 타이틀 효과 텍스트 제거 (공간 절약)
- ✅ 호버 시 상세 정보 툴팁으로 표시
- ✅ 폰트 크기 최소화: 0.7rem
- ⚠️ **대안**: 너비를 늘려서 가로 배치 유지 가능 (아래 옵션 참고)

---

### 4. DaevanionCard (69px 고정)

#### 현재 구조
- 헤더: "주신 능력치" + 활성화 개수 (1.125rem)
- 6개 신 그리드 (repeat(3, 1fr), 2행)
- 아이콘 이모지, 이름
- 하단 요약 정보

#### 개선안 (69px 내 최적화) - **초간결 모드**

```css
.daevanion-card {
  height: 69px;
  padding: 1rem;
  box-sizing: border-box;
}

/* 헤더 (20px) */
.daevanion-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 0.4rem;  /* 1rem → 0.4rem */
  height: 20px;
}

.daevanion-header h3 {
  font-size: 0.85rem;  /* 1.125rem → 0.85rem */
}

.daevanion-count {
  font-size: 0.7rem;  /* 0.75rem → 0.7rem */
  padding: 0.15rem 0.4rem;  /* 0.25rem 0.75rem → 축소 */
}

/* 신 그리드 - 1행으로 변경 (33px) */
.god-grid {
  display: flex;  /* grid → flex (가로 1줄) */
  gap: 0.3rem;
  height: 33px;
  justify-content: space-between;
}

/* 각 신 카드 */
.god-card {
  width: calc((100% - 1.5rem) / 6);  /* 6개 균등 분할 */
  aspect-ratio: 1;  /* 정사각형 유지 */
  padding: 0.2rem;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.god-icon {
  font-size: 0.9rem;  /* 1.5rem → 0.9rem */
  margin-bottom: 0;  /* 간격 제거 */
}

.god-name {
  font-size: 0.55rem;  /* 0.625rem → 0.55rem */
  display: none;  /* 이름 숨김 (공간 절약) */
}

/* 요약 정보 제거 */
.summary-stats {
  display: none;  /* 공간 부족으로 제거 */
}

/* 대안: 호버 시 상세 정보 표시 */
.god-card:hover {
  transform: scale(1.1);
}

.god-card:hover .god-name {
  display: block;
}
```

**변경 포인트:**
- ✅ 2행 그리드 → 1행 플렉스 박스
- ✅ 아이콘 크기 축소: 1.5rem → 0.9rem
- ✅ 신 이름 기본 숨김, 호버 시 표시
- ✅ 하단 요약 정보 제거
- ⚠️ **매우 컴팩트**: 가독성 한계선

---

## 🔄 대안: 카드 너비 확장 옵션

**문제**: TitleCard와 DaevanionCard가 너무 작아서 가독성 저하

### 해결책: 우측 컬럼 너비 확장

```css
/* 기존 */
.grid-container {
  grid-template-columns: 280px minmax(400px, 1fr) 400px;
}

/* 개선 (옵션 A: 우측 확장) */
.grid-container {
  grid-template-columns: 280px minmax(400px, 1fr) 450px;
  /* 400px → 450px (12.5% 증가) */
}

/* 개선 (옵션 B: 좌측 축소) */
.grid-container {
  grid-template-columns: 260px minmax(400px, 1fr) 420px;
  /* 좌측 280px → 260px, 우측 400px → 420px */
}
```

#### 옵션 A 채택 시 재계산 (우측 450px)

```
TitleCard (102px → 115px):
- 카테고리 카드 너비 증가로 텍스트 여유 공간 확보
- 대표 타이틀 효과 1줄 표시 가능

DaevanionCard (69px → 80px):
- 신 이름 표시 가능
- 요약 정보 복원 가능
```

---

## 📱 반응형 설계

### 브레이크포인트

```css
/* Desktop (1024px 이상) */
@media (min-width: 1024px) {
  .grid-container {
    display: grid;
    grid-template-columns: 280px minmax(400px, 1fr) 450px;
    gap: 1.5rem;
  }

  .right-column .card {
    /* 고정 높이 적용 */
    height: var(--card-height);
  }
}

/* Tablet (768px ~ 1023px) */
@media (min-width: 768px) and (max-width: 1023px) {
  .grid-container {
    display: grid;
    grid-template-columns: 1fr 1fr;  /* 2열 */
    gap: 1rem;
  }

  .right-column {
    grid-column: 2;
  }

  .center-column {
    grid-column: 1;
    grid-row: 2;
  }

  /* 카드 높이 자동 조정 */
  .right-column .card {
    height: auto;
  }
}

/* Mobile (767px 이하) */
@media (max-width: 767px) {
  .grid-container {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  /* 모든 카드 높이 자동 */
  .card {
    height: auto;
  }
}
```

---

## 🎯 최종 권장 설계

### 옵션 1: 고정 높이 + 컴팩트 디자인 (추천)

**장점:**
- 깔끔한 정렬
- 공간 효율성
- 일관된 레이아웃

**단점:**
- 일부 정보 축소/생략 필요
- TitleCard, DaevanionCard 가독성 한계

**적용 방법:**
1. 우측 컬럼 너비 450px로 확장
2. 위 설계대로 각 카드 높이 고정
3. 폰트 크기 조정
4. 부가 정보는 호버/툴팁으로 표시

---

### 옵션 2: 유동 높이 + 최소 높이 설정

**장점:**
- 모든 정보 표시 가능
- 가독성 최고

**단점:**
- 정렬 불일치
- 공간 낭비

**적용 방법:**
1. 각 카드에 min-height만 설정
2. 내용에 따라 자동 확장
3. ProfileSection과의 정렬은 포기

---

### 옵션 3: 하이브리드 (균형)

**장점:**
- 정렬 + 가독성 균형
- 유연한 레이아웃

**적용 방법:**
1. MainStatsCard, RankingCard: 고정 높이 (중요도 높음)
2. TitleCard, DaevanionCard: 자동 높이 (정보량 적음)
3. ProfileSection 하단 여백으로 높이 조정

```css
.right-column {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.main-stats-card {
  height: 220px;  /* 고정 */
  flex-shrink: 0;
}

.ranking-card {
  height: 240px;  /* 고정 */
  flex-shrink: 0;
}

.title-card,
.daevanion-card {
  height: auto;  /* 자동 */
  min-height: 120px;  /* 최소 높이 */
}

/* ProfileSection에 하단 여백 추가로 높이 맞춤 */
.profile-section {
  padding-bottom: calc(1.5rem + var(--bottom-padding));
}
```

---

## 📋 구현 체크리스트

### Phase 1: 기본 구조 조정
- [ ] 우측 컬럼 너비 450px로 확장
- [ ] 각 카드 고정 높이 CSS 변수 정의
- [ ] 카드 간 간격 1rem 통일

### Phase 2: MainStatsCard 리팩토링
- [ ] 헤더 폰트 크기 0.95rem
- [ ] 아이콘 크기 22px
- [ ] 스텟 라벨 0.7rem
- [ ] 스텟 값 0.85rem
- [ ] 패딩 최적화

### Phase 3: RankingCard 리팩토링
- [ ] 헤더 폰트 크기 0.95rem
- [ ] 아이콘 크기 24px
- [ ] 아이템 간격 0.4rem
- [ ] 스크롤 추가

### Phase 4: TitleCard 리팩토링
- [ ] 헤더 폰트 크기 0.9rem
- [ ] 아이콘 크기 16px
- [ ] 대표 타이틀 효과 1줄만 표시
- [ ] 호버 툴팁 추가

### Phase 5: DaevanionCard 리팩토링
- [ ] 헤더 폰트 크기 0.85rem
- [ ] 신 아이콘 0.9rem
- [ ] 1행 레이아웃으로 변경
- [ ] 요약 정보 간소화

### Phase 6: 반응형 테스트
- [ ] Desktop (1024px+) 테스트
- [ ] Tablet (768px~1023px) 테스트
- [ ] Mobile (767px 이하) 테스트

### Phase 7: 가독성 검증
- [ ] 최소 폰트 크기 0.7rem 확인
- [ ] 터치 영역 44px 확인
- [ ] 색상 대비 WCAG AA 준수

---

## 💡 추가 개선 아이디어

### 1. 접기/펼치기 기능
- 각 카드에 접기 버튼 추가
- 접힌 상태에서는 헤더만 표시
- 사용자가 원하는 카드만 확장

### 2. 드래그 앤 드롭 정렬
- 사용자가 카드 순서 변경 가능
- LocalStorage에 설정 저장

### 3. 컴팩트 모드 토글
- "컴팩트 뷰" 버튼 추가
- 클릭 시 모든 카드 최소 높이로 축소
- 정보 밀도 증가

---

## 🎨 디자인 토큰 정의

```css
:root {
  /* 카드 높이 */
  --profile-section-height: 639px;
  --main-stats-card-height: 220px;
  --ranking-card-height: 240px;
  --title-card-height: 120px;
  --daevanion-card-height: 90px;

  /* 컬럼 너비 */
  --left-column-width: 280px;
  --right-column-width: 450px;
  --center-column-min: 400px;

  /* 간격 */
  --card-gap: 1rem;
  --card-padding: 1rem;

  /* 폰트 크기 */
  --font-size-header: 0.95rem;
  --font-size-body: 0.8rem;
  --font-size-small: 0.7rem;

  /* 아이콘 크기 */
  --icon-size-large: 24px;
  --icon-size-medium: 20px;
  --icon-size-small: 16px;
}
```

---

## 📸 Before / After 비교

### Before
```
ProfileSection: 639px
├─ MainStatsCard: 280px (너무 큼)
├─ RankingCard: 350px (너무 큼)
├─ TitleCard: 220px (적당)
└─ DaevanionCard: 180px (적당)
─────────────────────────
총 높이: 1030px (불일치!)
```

### After (옵션 1)
```
ProfileSection: 639px
├─ MainStatsCard: 220px ✅
├─ RankingCard: 240px ✅
├─ TitleCard: 120px ✅
└─ DaevanionCard: 90px ✅
─────────────────────────
총 높이: 670px + 간격 = ~700px
(하단 여백으로 조정 가능)
```

---

## 🚀 배포 전 최종 점검

- [ ] 모든 카드 높이 합계 = ProfileSection 높이
- [ ] 폰트 크기 최소 0.7rem 이상
- [ ] 아이콘 크기 최소 16px 이상
- [ ] 터치 영역 최소 44px 이상
- [ ] 색상 대비율 4.5:1 이상
- [ ] 스크롤 영역 명확히 표시
- [ ] 반응형 모든 브레이크포인트 테스트
- [ ] 크로스 브라우저 테스트 (Chrome, Firefox, Safari)
- [ ] 접근성 테스트 (Screen Reader)

---

## 📝 마무리

이 설계는 **가독성과 공간 효율의 균형**을 목표로 합니다.

**핵심 포인트:**
1. ✅ ProfileSection을 기준으로 우측 카드 높이 조정
2. ✅ 일관된 패딩, 간격, 폰트 크기 시스템
3. ✅ 필수 정보 유지, 부가 정보는 툴팁/호버로
4. ✅ 반응형 대응으로 모든 화면에서 최적화
5. ✅ 최소 가독성 기준 준수 (폰트 0.7rem, 아이콘 16px)

사용자가 한눈에 모든 정보를 파악할 수 있도록 **시각적 일관성**을 최우선으로 고려했습니다.
